<Window x:Class="ytlivechatwedus.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="YouTube Secure Sign-In" 
        Height="650" Width="520"
        Background="#121214"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Status Panel -->
            <RowDefinition Height="*"/>    <!-- Browser Area -->
        </Grid.RowDefinitions>

        <!-- Status Panel -->
        <Border Grid.Row="0" Background="#1A1A1E" BorderBrush="#2A2A32" BorderThickness="0,0,0,1" Padding="12,10">
            <StackPanel>
                <TextBlock FontSize="13" FontWeight="Bold" Foreground="White" Margin="0,0,0,2">
                    <Run Text="Secure Google / YouTube Authentication" Foreground="#00f2fe"/>
                </TextBlock>
                <TextBlock Text="Log in with your email/password. After successfully logging in and seeing your YouTube home page, simply close this window." 
                           Foreground="#A3A3B8" FontSize="10.5" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Browser Area -->
        <wv2:WebView2 x:Name="webView" Grid.Row="1"/>
    </Grid>
</Window>
